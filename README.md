# NARX-MSD
NARX framework for discovering space representations of dynamic systems

create a virtual environment python -m venv narx
.venv\Scripts\activate

install dependecies
pip install torch
pip install pandas 
pip install numpy
pip install matplotlib